CREATE TABLE books (
    id INT PRIMARY KEY,
    title VARCHAR(255),
    url VARCHAR(255),
    description TEXT
);
CREATE TABLE exercises (
    id INT PRIMARY KEY,
    title VARCHAR(255),
    url VARCHAR(255),
    description TEXT
);